describe("renders the search_results page", () => {
    it("renders correctly", () => {
        cy.visit("/admin_reviews")
    }) 
    // it("contains Approve option for admin", () => {
    //     cy.get('div').contains("Approve")
    // }) 
    // it("contains Reject option for admin", () => {
    //     cy.get('div').contains("Reject")
    // }) 
}) 